# decision/2026-08-30-kit-sync-adopts-the-upstream-62-fix-the-local-encoding-fork-is-retired
Date: 2026-08-30
Anchor: 2026-08-30 — /kit-sync adopts the upstream #62 fix; the local encoding fork is retired
Status: accepted

## Claim
Fast-forwarded `~/.agent-kit` (`d57880d` → `2f9c664`) and took all 5 `Divergent-Skipped` tool scripts from the kit outright — `Update-WorkMirror.ps1` + `.Tests.ps1`, `Test-DesignState.ps1` + `.Tests.ps1`, `Update-DesignProjection.Tests.ps1` — plus 4 trivial core command updates and 2 new test files `Sync-Kit.ps1` already classified `Updated`/`Added`. This repo's own UTF-8 subprocess-decoding fix for `gh` output survived unchanged in spirit under the kit's `Invoke-GhRaw` name in every file, so nothing local was lost; the kit's newer versions carry the actual #62 fix (`Update-WorkMirror.ps1` writes a record only when `Title`/`State`/`Rank`/`Criteria` changed — `MirroredAt` alone never triggers a write) plus #113 and #161. Restored this repo's own S18.6 test body verbatim (its own decision-file path and its freeze-conditional expectation from the 2026-08-25 entry) rather than accepting the kit's simplified version, which would have reintroduced the exact regression that entry paid down. Added `AGENTS.md § Writing a design-state record` verbatim from the kit, since the newly-taken core commands now cite it and this repo's `AGENTS.md` never had it. Added a matching skip guard to this repo's own `tools/Test-CIWorkflow.Tests.ps1` so its `S12.7` self-test accurately reports satisfied here. Verified live: two consecutive `Update-WorkMirror.ps1` runs both wrote 0 records; full Pester suite 343/343; `Test-DesignState.ps1` and `Test-DesignDrift.ps1` both exit 0.
