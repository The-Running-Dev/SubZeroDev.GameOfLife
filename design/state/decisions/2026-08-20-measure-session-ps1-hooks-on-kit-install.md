# decision/2026-08-20-measure-session-ps1-hooks-on-kit-install
Date: 2026-08-20
Anchor: 2026-08-20 — Measure-Session.ps1 hooks on kit install
Status: accepted

## Claim
Install both hooks. Created `.claude/settings.json` containing only `hooks.SessionEnd` and `hooks.UserPromptSubmit`, matching the kit's own settings.json verbatim (repo has no `permissions`/`model` keys to protect).
